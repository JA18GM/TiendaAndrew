import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
//import { PagoComponent } from './pago/pago.component';

@NgModule({
  declarations: [
    // Componentes, directivas, pipes que pertenecen al módulo PayPal
  
    //PagoComponent
  ],
  imports: [
    CommonModule
  ],
  exports: [
    // Componentes, directivas, pipes que deben ser accesibles desde otros módulos
  ]
})
export class PayPalModule { }
